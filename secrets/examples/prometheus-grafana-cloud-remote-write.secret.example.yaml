# Example only — copy to repo root as prometheus-grafana-cloud-secret.local.yaml (gitignored)
# or use: read -s ... && kubectl create secret (preferred; see secrets/README.md).
apiVersion: v1
kind: Secret
metadata:
  name: prometheus-grafana-cloud-remote-write
  namespace: monitoring
type: Opaque
stringData:
  api-key: REPLACE_WITH_GRAFANA_CLOUD_METRICS_WRITE_TOKEN
