apiVersion: v1
kind: Service
metadata:
  name: layer-orchestrator
  labels:
    environment: dev
spec:
  type: NodePort
  selector:
    app: layer-orchestrator
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30184
