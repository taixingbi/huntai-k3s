apiVersion: v1
kind: Service
metadata:
  name: layer-gateway-inference
  labels:
    environment: dev
spec:
  type: NodePort
  selector:
    app: layer-gateway-inference
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30180
