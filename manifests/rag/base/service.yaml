apiVersion: v1
kind: Service
metadata:
  name: layer-rag-query
  labels:
    environment: dev
spec:
  type: NodePort
  selector:
    app: layer-rag-query
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30183
