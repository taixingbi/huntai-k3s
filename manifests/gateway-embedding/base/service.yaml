apiVersion: v1
kind: Service
metadata:
  name: layer-gateway-embedding
  labels:
    environment: dev
spec:
  type: NodePort
  selector:
    app: layer-gateway-embedding
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30181
