apiVersion: apps/v1
kind: Deployment
metadata:
  name: layer-gateway-api
  labels:
    environment: prod
spec:
  template:
    metadata:
      labels:
        environment: prod
    spec:
      containers:
        - name: gateway
          envFrom:
            - secretRef:
                name: layer-ai-prod-secrets
          env:
            - name: ENV
              value: "prod"
            - name: FRONTEND_URL
              value: "https://taixingai.com"
            - name: ADDITIONAL_FRONTEND_URLS
              value: "https://www.taixingai.com,http://192.168.86.179:30386"
            - name: ORCHESTRATOR_TIMEOUT_MS
              value: "120000"
