apiVersion: v1
kind: Service
metadata:
  name: layer-gateway-api
  labels:
    environment: dev
    workload: gateway-api
spec:
  type: NodePort
  selector:
    app: layer-gateway-api
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30185
